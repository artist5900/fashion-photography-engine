# Garment Image Examples

This folder is reserved for local product images used with Garment Parser v1.

The committed example manifest uses descriptive image paths so the parser can demonstrate its schema without storing large binary image assets in the repository.

Add real product images here locally, then run:

```bash
python3 src/garment_parser.py examples/garment_input.json --out examples/garment.json
```

